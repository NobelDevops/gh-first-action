name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - print greeting
        run: echo "hello World"
      - name: print goodbye 
        run" echo "done"
      
  

